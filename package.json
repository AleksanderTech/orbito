{
  "name": "orbito",
  "version": "0.0.2",
  "type": "module",
  "description": "Orbito is a static site generator (SSG) written in JavaScript using Node.js.",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon --watch src --watch index.js --exec npm run serve",
    "serve": "npm run build && serve out",
    "build": "rm -rf out/** && node index.js"
  },
  "keywords": [
    "SSG",
    "static site generator"
  ],
  "author": "Aleksander Roztropiński",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.20.1",
    "marked": "^12.0.1",
    "nodemon": "^3.1.0",
    "serve": "^14.2.1",
    "tailwindcss": "^3.4.1"
  }
}
